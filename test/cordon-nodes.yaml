apiVersion: maintenance.nmoo.io/v1alpha1
kind: NodeMaintenancePlan
metadata:
  name: cordon-workers
spec:
  reason: "Planned maintenance"

  nodeSelector:
    matchLabels:
      tier: gpu
  cordon:
    enabled: true
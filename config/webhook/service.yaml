apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: node-maintenance-orchestrator-system
spec:
  selector:
    control-plane: controller-manager
  ports:
    - port: 443
      targetPort: 9443

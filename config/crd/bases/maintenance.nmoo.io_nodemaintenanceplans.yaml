---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodemaintenanceplans.maintenance.nmoo.io
spec:
  group: maintenance.nmoo.io
  names:
    kind: NodeMaintenancePlan
    listKind: NodeMaintenancePlanList
    plural: nodemaintenanceplans
    shortNames:
    - nmp
    singular: nodemaintenanceplan
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.nodes.size()
      name: Nodes
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Cordoned')].status
      name: Cordoned
      type: string
    - jsonPath: .status.conditions[?(@.type=='DrainInProgress')].status
      name: Draining
      type: string
    - jsonPath: .status.driftedNodes
      name: Drift
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeMaintenancePlanSpec defines desired state
            properties:
              cordon:
                description: Cordon behavior
                properties:
                  enabled:
                    default: false
                    description: Whether cordon should be applied
                    type: boolean
                  startAt:
                    description: Time at which cordon should begin
                    format: date-time
                    type: string
                type: object
              drain:
                description: Drain behavior
                properties:
                  enabled:
                    default: false
                    description: Whether draining should be performed
                    type: boolean
                  options:
                    description: Drain options
                    properties:
                      force:
                        default: false
                        description: Force delete pods not backed by controllers
                        type: boolean
                      ignoreDaemonSets:
                        default: true
                        description: Ignore DaemonSet-managed pods
                        type: boolean
                      ignoreStatefulSets:
                        default: false
                        description: Ignore pods managed by StatefulSets
                        type: boolean
                      maxParallel:
                        default: 1
                        description: Max number of nodes drained in parallel
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: Timeout for pod eviction (seconds)
                        format: int64
                        minimum: 0
                        type: integer
                    type: object
                  startAt:
                    description: Time at which drain should begin
                    format: date-time
                    type: string
                type: object
              nodeSelector:
                description: |-
                  Label selector for nodes
                  Mutually exclusive with Nodes
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              nodes:
                description: |-
                  Explicit list of node names
                  Mutually exclusive with NodeSelector
                items:
                  type: string
                type: array
              reason:
                description: Reason for maintenance (added as annotation to nodes)
                type: string
            type: object
            x-kubernetes-validations:
            - message: cordon.startAt must be before or equal to drain.startAt
              rule: '!has(self.cordon) || !has(self.drain) || !has(self.cordon.startAt)
                || !has(self.drain.startAt) || self.cordon.startAt <= self.drain.startAt'
            - message: cannot set both nodes and nodeSelector
              rule: '!(has(self.nodes) && has(self.nodeSelector))'
          status:
            description: NodeMaintenancePlanStatus defines observed state
            properties:
              conditions:
                description: Conditions represent current state of the plan
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              driftedNodes:
                description: List of nodes with detected drift
                items:
                  type: string
                type: array
              lastError:
                description: Last error message if preview or execution failed
                type: string
              lastPreviewTime:
                description: Last time preview was computed
                format: date-time
                type: string
              nodes:
                description: Per-node status
                items:
                  description: NodeStatus represents per-node observed state
                  properties:
                    blockedPods:
                      format: int32
                      type: integer
                    cordoned:
                      type: boolean
                    drainProgress:
                      format: int32
                      type: integer
                    driftReason:
                      type: string
                    drifted:
                      type: boolean
                    evictablePods:
                      format: int32
                      type: integer
                    issues:
                      description: Issues detected during preview or execution
                      items:
                        description: NodeIssue represents a detected issue
                        properties:
                          message:
                            description: Human-readable message
                            type: string
                          podRef:
                            description: Related pod if applicable
                            properties:
                              name:
                                type: string
                              namespace:
                                type: string
                            type: object
                          type:
                            description: Type of issue (e.g. PDBBlocked, Unschedulable,
                              DriftDetected)
                            type: string
                        required:
                        - message
                        - type
                        type: object
                      type: array
                    name:
                      type: string
                    totalPods:
                      format: int32
                      type: integer
                    unschedulablePods:
                      format: int32
                      type: integer
                  required:
                  - name
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects latest reconciled spec
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

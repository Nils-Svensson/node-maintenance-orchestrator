---
# Permissions required for the operator's self-signed cert bootstrapper.
# The bootstrapper stores a shared CA in a Secret regardless of which server
# certs are generated, so this Role is needed for both webhook and metrics TLS.
# Scoped to the specific webhook config name to limit blast radius.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cert-bootstrapper-role
rules:
  - apiGroups: ["admissionregistration.k8s.io"]
    resources: ["validatingwebhookconfigurations"]
    resourceNames: ["node-maintenance-orchestrator-validating-webhook-configuration"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cert-bootstrapper-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cert-bootstrapper-role
subjects:
  - kind: ServiceAccount
    name: controller-manager
    namespace: system
---
# Secret access is namespace-scoped.
# create must be in a separate rule without resourceNames — Kubernetes RBAC does
# not evaluate resourceNames for create requests (the resource has no name yet).
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cert-bootstrapper-secret-role
  namespace: system
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create"]
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["node-maintenance-orchestrator-tls-cert"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: cert-bootstrapper-secret-rolebinding
  namespace: system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: cert-bootstrapper-secret-role
subjects:
  - kind: ServiceAccount
    name: controller-manager
    namespace: system

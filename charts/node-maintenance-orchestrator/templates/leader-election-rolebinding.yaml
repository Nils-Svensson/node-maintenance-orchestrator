apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "nmo.fullname" . }}-leader-election-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nmo.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "nmo.fullname" . }}-leader-election-role
subjects:
  - kind: ServiceAccount
    name: {{ include "nmo.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
